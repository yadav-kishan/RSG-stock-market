{
  "name": "fox-trading-platform",
  "version": "1.0.0",
  "description": "Full-stack trading and network marketing platform",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix api & npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix api",
    "install:all": "npm install --prefix frontend && npm install --prefix api",
    "postinstall": "npm run install:all"
  },
  "workspaces": [
    "frontend",
    "api"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}